def sum_of_list(nums):
    """Return the sum of the numbers in nums, using recursion.

    No sum(), no for, no while — see the README.
    Think about the base case (the simplest possible list) and how each
    recursive call makes the problem smaller.
    """
    pass


# Stretch: sum all numbers even inside nested lists of arbitrary depth.
# def deep_sum(nums):
#     """Return the sum of all numbers in nums, including nested lists.
#
#     deep_sum([1, [2, [3, 4]], 5])  # 15
#     """
#     pass
